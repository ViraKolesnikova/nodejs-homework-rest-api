const { contactValidation, favoriteValidation } = require('./contactsValidation');
const { userValidation } = require('./userValidation');

module.exports = {
  contactValidation,
  favoriteValidation,
  userValidation
}